name: Application Deployment on AWS

on:
  workflow_dispatch:

jobs:
  deployment:
    runs-on: ubuntu-latest

    steps:
      - name: Display Success Message
        run: echo "Application deployed to AWS successfully. ✔"