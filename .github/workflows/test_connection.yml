name: Test connection for pipeline

on:
  workflow_dispach:

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "Github Action is working successfully ✔"