name: Test Fleet health and Env Variable

on:
  workflow_dispach:

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "Github Action is working successfully ✔"